// Development environment configuration.
// Angular CLI replaces this file with environment.prod.ts on production builds.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:5205/api'   // Matches launchSettings.json http profile
};